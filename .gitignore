node_modules/
dist/
.env
.env.local
packages/contracts/.env
apps/demo/.next/*

# Hardhat
artifacts/
cache/
coverage/
coverage.json
deployments/localhost/
typechain-types/

# Zama Relayer SDK WASM binaries — copied to public/ by postinstall, not committed
apps/demo/public/tfhe_bg.wasm
apps/demo/public/kms_lib_bg.wasm
apps/demo/public/workerHelpers.js

# FHE artefacts
# - anomaly_model.json     : large compiled weights — excluded, regenerate with `pnpm models:compile`
# - anomaly_model.manifest.json : schema/metadata — committed so the server knows the feature schema
# - *.fhe / *.params       : raw Concrete binary circuits — excluded
packages/models/artifacts/anomaly_model.json
packages/models/artifacts/*.fhe
packages/models/artifacts/*.params

# Python
__pycache__/
*.pyc
.venv/

# Misc
.DS_Store
*.log

# AI
.claude/
