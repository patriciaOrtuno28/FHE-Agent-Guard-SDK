# concrete-ml is already included in the zamafhe/concrete-ml Docker base image.
# These are additional deps only.
numpy==1.23.5
pandas==2.0.3
pyarrow>=15.0.0
scikit-learn==1.1.3
# For local installation (Linux/WSL only, not supported on Windows natively):
# pip install "concrete-ml==1.7.0"